apiVersion: plugins.weknora.io/v1alpha1
kind: Plugin
metadata:
  id: community.local-directory
  name: Local Directory
  version: 0.1.0
spec:
  protocolVersion: "1.0"
  compatibility:
    weknora: ">=0.8.0 <0.9.0"
  extension:
    id: local_directory
    type: datasource
    contractVersion: "1.0"
    capabilities: [resource_listing, full_sync, incremental_sync, deletion_events]
  runtime:
    kind: sandbox_service
    transport: uds
    entrypoint: bin/plugin-linux-amd64
  permissions:
    network: none
    filesystem: selected_directory_readonly
  resources:
    memoryMiB: 256
    cpuQuota: 0.5
    maxProcesses: 32
  configSchema:
    type: object
    additionalProperties: false
